<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="illustrator"
            type="com.example.android_firebase_2.models.Illustrator"/>
    </data>


    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <ImageView
            android:id="@+id/illustratorImage"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:scaleType="centerCrop"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="16dp"
            android:src="@{illustrator.imagen}" />

        <TextView
            android:id="@+id/illustratorTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{illustrator.titulo}"
            android:textSize="16sp"
            android:textColor="@android:color/black"
            android:layout_toEndOf="@id/illustratorImage"
            android:layout_centerVertical="true" />

    </RelativeLayout>
</layout>

